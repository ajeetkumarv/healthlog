create table facility
(
	id INTEGER PRIMARY KEY AUTO_INCREMENT,
    name varchar(60),
    address varchar(200),
    city varchar(20),
    state varchar(10),
    country varchar(20),
    zip int unsigned
    
);